{
    "version": "0.2.0",
    "configurations": [
    {
        "name": "Debug with CUDA GDB",
        "type": "cuda-gdb",
        "request": "launch",
        "program": "${workspaceFolder}/build/${fileBasenameNoExtension}",
        "args": [],
        "environment": [
            {
                "name": "CUDBG_USE_LEGACY_DEBUGGER",
                "value": "1"
            }
        ]
    }
    ]
}